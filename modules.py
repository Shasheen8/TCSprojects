# Used to organise our code. we can take code from different files and import it in the main file#check app.py file fo the main info#1st way to importfrom typing import Listimport appprint(app.kg_to_lbs(100))#2nd way to importfrom app import lbs_to_kglbs_to_kg(300)#importing exonimportingfrom Exonimporting import find_maxnumbers =[2,5,3,4,5,6,6,4]max = find_max(numbers)print(max)